export const emptyColumnsStyles = {
    control: (base) => ({
        ...base,
        '&:hover, &:active': {
            borderColor: 'red',
        },
        border: '1px solid red',
        boxShadow: 'none',
    }),
};
